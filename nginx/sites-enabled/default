server {
    listen __PORT__ default_server;
    server_name  _;

    # location / {
    #     include uwsgi_params;
    #     uwsgi_pass unix:/tmp/uwsgi.sock;
    # }
    location / {
        root /usr/src/app/;
    }
}
